import { ImageUtilPipe } from './image-util.pipe';

describe('ImageUtilPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageUtilPipe();
    expect(pipe).toBeTruthy();
  });
});
